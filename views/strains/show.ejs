<% include ../partials/header %>

<div class="container">
  <div class="thumbnail">
    <img src="<%= strain.image %>" alt="<%= strain.name %>">
    <h2><%= strain.name %></h2>
    <p><%= strain.type %></p>
    <p><%= strain.description %></p>
    <a href="<%= strain._id %>/edit">Edit Strain</a>
  </div>
  <a href="<%= strain._id %>/reviews/new">Leave a review</a>
  <% strain.reviews.forEach((review) => { %>
    <p><%= review.text %></p>
    <p><%= review.rating %></p>
  <% }) %>
</div>

<% include ../partials/footer %>